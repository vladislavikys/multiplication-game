# 🔐 Настройка системы кодов активации

## 📋 Что было реализовано

✅ **Безопасная система кодов активации:**
- 1 код = 1 пользователь (код можно использовать только один раз)
- Коды хранятся на Firebase (защищены правилами безопасности)
- Только администратор может создавать коды
- История активаций сохраняется в аккаунте пользователя навсегда
- Защита от повторного использования на уровне базы данных

---

## 🚀 Шаг 1: Настройка Firebase Security Rules

### 1.1. Откройте Firebase Console
- Перейдите: https://console.firebase.google.com/
- Выберите проект: **multiplication-game-22120**

### 1.2. Настройте правила Firestore
1. В левом меню выберите **Firestore Database**
2. Перейдите на вкладку **Rules** (Правила)
3. **ЗАМЕНИТЕ ВСЕ** текущие правила на содержимое файла `firestore.rules`
4. **ВАЖНО:** В файле `firestore.rules` найдите строку:
   ```
   'your-admin-email@example.com'  // ЗАМЕНИТЕ НА ВАШ EMAIL АДМИНА!
   ```
   И замените на ваш реальный email администратора (тот, под которым вы будете добавлять коды)

5. Нажмите **Publish** (Опубликовать)

### Пример правил:
```javascript
match /codes/{codeId} {
  allow create: if request.auth != null
    && request.auth.token.email in [
      'admin@example.com'  // ← ВАШИХ EMAIL АДМИНА
    ];
}
```

---

## 🎯 Шаг 2: Создание администратора

### 2.1. Зарегистрируйте админский аккаунт
1. Откройте ваш сайт (index.html)
2. Зарегистрируйте новый аккаунт с email, который вы указали в правилах
3. Например: `admin@example.com` / `ваш_пароль`

### 2.2. Проверьте права
1. Откройте файл `admin-secret-panel-xyz789.html` в браузере
2. Войдите под админским аккаунтом
3. Попробуйте создать тестовый код

---

## 🔑 Шаг 3: Создание кодов активации

### 📍 Как открыть админ-панель:

**На Netlify или любом хостинге:**
```
https://ваш-сайт.netlify.app/admin-secret-panel-xyz789.html
```

**Локально:**
- Просто откройте файл `admin-secret-panel-xyz789.html` в браузере
- Или через локальный сервер: `http://localhost:8080/admin-secret-panel-xyz789.html`

⚠️ **ВАЖНО:** Никому не сообщайте этот URL! Это секретная админ-панель.

### Вариант 1: Через админ-панель (рекомендуется)
1. Откройте `admin-secret-panel-xyz789.html` в браузере
2. Войдите под админским аккаунтом
3. Используйте форму для создания кодов:
   - **Одиночное создание:** Введите код или сгенерируйте случайный (кнопка 🎲)
   - **Массовое создание:** Укажите количество кодов и игр на каждый

### Вариант 2: Через консоль Firebase
1. Откройте Firebase Console → Firestore Database
2. Перейдите в коллекцию `codes`
3. Нажмите **Add document**
4. Заполните:
   ```
   Document ID: GAME-2025 (ваш код)

   Поля:
   - games (number): 10
   - used (boolean): false
   - usedBy (string): null
   - usedAt (timestamp): null
   - createdAt (timestamp): [текущее время]
   - createdBy (string): admin@example.com
   ```

---

## 📱 Шаг 4: Как пользователи активируют коды

1. Пользователь регистрируется и входит в аккаунт
2. На главном экране нажимает **"🔑 Добавить игры"**
3. Вводит код активации (например: `GAME-2025`)
4. Нажимает **"Активировать"**

### Что происходит:
✅ Проверяется, что код существует
✅ Проверяется, что код не использован
✅ Проверяется, что пользователь не активировал этот код ранее
✅ Код помечается как использованный и привязывается к пользователю
✅ Игры добавляются на аккаунт пользователя **НАВСЕГДА**
✅ Код сохраняется в историю активаций пользователя

---

## 🔒 Безопасность

### Что защищено:
- ✅ Только админы могут создавать коды (защита на уровне Firebase)
- ✅ Обычные пользователи могут только читать и активировать коды
- ✅ Один код = один пользователь (защита от повторного использования)
- ✅ Транзакции гарантируют целостность данных
- ✅ История активаций хранится навсегда

### Структура данных:

**Коллекция `codes`:**
```javascript
{
  "GAME-2025": {
    games: 10,              // Количество игр
    used: false,            // Использован ли код
    usedBy: null,           // ID пользователя (если использован)
    usedAt: null,           // Дата использования
    createdAt: timestamp,   // Дата создания
    createdBy: "admin@example.com"  // Кто создал
  }
}
```

**Коллекция `users`:**
```javascript
{
  "userId123": {
    gamesLeft: 25,          // Текущее количество игр
    activatedCodes: [       // История активированных кодов
      "GAME-2025",
      "PROMO-100"
    ],
    lastActivation: timestamp,
    createdAt: timestamp
  }
}
```

---

## 🧪 Тестирование

### Тест 1: Создание кода
1. Откройте `admin-secret-panel-xyz789.html`
2. Создайте код `TEST-0001` на 5 игр
3. Проверьте, что код появился в таблице

### Тест 2: Активация кода
1. Откройте `index.html`
2. Зарегистрируйте тестового пользователя
3. Активируйте код `TEST-0001`
4. Проверьте, что добавилось 5 игр

### Тест 3: Повторная активация (должна быть заблокирована)
1. Попробуйте активировать `TEST-0001` еще раз
2. Должна появиться ошибка: "Вы уже активировали этот код!"

### Тест 4: Активация использованного кода другим пользователем
1. Создайте второго пользователя
2. Попробуйте активировать `TEST-0001`
3. Должна появиться ошибка: "Код уже использован другим пользователем!"

---

## 📊 Админ-панель (admin-secret-panel-xyz789.html)

### Возможности:
- 📈 **Статистика:** Всего кодов / Доступно / Использовано / Всего игр
- ➕ **Создание кодов:** Одиночное и массовое
- 🎲 **Генератор кодов:** Автоматическая генерация уникальных кодов
- 📋 **Список всех кодов:** С фильтрацией и статусами
- 🗑️ **Удаление:** Можно удалить только неиспользованные коды
- 📅 **История:** Кто и когда использовал код

---

## ⚠️ Важные замечания

1. **Email администратора:**
   - ОБЯЗАТЕЛЬНО замените в `firestore.rules` на свой реальный email
   - Без этого вы не сможете создавать коды

2. **Развертывание:**
   - После изменения `firestore.rules` нужно опубликовать правила в Firebase Console
   - Изменения применяются мгновенно

3. **Безопасность:**
   - URL админ-панели защищен: `admin-secret-panel-xyz789.html` (сложно угадать)
   - Поисковые системы не индексируют админ-панель (robots.txt + meta noindex)
   - Используйте сильный пароль для админского аккаунта
   - Никому не сообщайте URL админ-панели

4. **Резервное копирование:**
   - Регулярно экспортируйте коды из Firestore
   - Храните резервные копии списка кодов

---

## 🎉 Готово!

Теперь у вас:
- ✅ Безопасная система кодов активации
- ✅ Админ-панель для управления кодами
- ✅ Защита от повторного использования
- ✅ История активаций пользователей
- ✅ Правила безопасности Firebase

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера (F12 → Console)
2. Проверьте правила Firebase
3. Убедитесь, что ваш email админа правильно указан в `firestore.rules`
4. Проверьте подключение к интернету
